{
  "name": "@sco-techlab/easy-files",
  "version": "10.1.5",
  "author": "SCO Techlab (Santiago Comeras Oteo)",
  "description": "Easy nodejs files management",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && nest build && cp ./package.json ./dist/easy-files && cp ./README.md ./dist/easy-files && chmod -R 777 ./dist",
    "publish": "npm publish dist/easy-files --access=public"
  },
  "repository": {},
  "dependencies": {},
  "devDependencies": { 
    "@types/multer": "^1.4.12"
  }
}
